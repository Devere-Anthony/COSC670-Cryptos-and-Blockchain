require("@nomicfoundation/hardhat-toolbox/network-helpers");
const { expect } = require("chai");

describe("Courses Offered:", function () {
    it("Adding courses", async function () {
        const cosc670 = await ethers.deployContract("CSEnrollment");
    });
});
